{% extends 'base_public.html.twig' %}

{% block title %}New Booking{% endblock %}

{% block body %}

    <div class="flex flex-col items-center p-4 space-y-6">

        <div class="flex justify-center">
            {{ include('booking/form.html.twig', {'form_title' : 'Create new  booking', 'button_label': 'Book'}) }}

        </div>
        <a href="{{ path('app_home') }}" class="btn btn-soft">Back to festivals</a>

    </div>

{% endblock %}
